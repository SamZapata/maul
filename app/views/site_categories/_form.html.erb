<%= form_for(site_category) do |f| %>
  <% if site_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site_category.errors.count, "error") %> prohibited this category site from being saved:</h2>

      <ul>
      <% site_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :about %>
    <%= f.text_area :about %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
